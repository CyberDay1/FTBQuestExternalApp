<ContentDialog
    x:Class="FTBQuestEditor.WinUI.Views.PresetsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:FTBQuestEditor.WinUI.ViewModels"
    Title="Presets"
    PrimaryButtonText="Close"
    DefaultButton="Primary"
    Opened="OnDialogOpened">
    <StackPanel Spacing="12">
        <TextBlock Text="Available Presets" FontWeight="SemiBold" />
        <ListView ItemsSource="{Binding Slots}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:PresetsDialogViewModel.PresetSlotViewModel">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind FilePath}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <Button
                            Grid.Column="1"
                            Content="Delete"
                            Click="OnDeleteSlotClicked"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</ContentDialog>
