<Page
    x:Class="FTBQuestEditor.WinUI.Views.GridViewPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:FTBQuestEditor.WinUI.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12" Padding="12" VerticalAlignment="Center">
            <TextBlock Text="Quest Grid" FontSize="18" FontWeight="SemiBold" />
            <TextBlock Text="Zoom:" />
            <TextBlock Text="{x:Bind ViewModel.Zoom, Mode=OneWay, StringFormat='{}{0:0.00}×'}" />
            <TextBlock Text="Selected:" />
            <TextBlock Text="{x:Bind ViewModel.SelectedIcons.Count, Mode=OneWay}" />
        </StackPanel>

        <Border Grid.Row="1" Margin="12" CornerRadius="8" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" BorderThickness="1">
            <Grid>
                <Canvas
                    x:Name="CanvasRoot"
                    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                    PointerPressed="Canvas_PointerPressed"
                    PointerMoved="Canvas_PointerMoved"
                    PointerReleased="Canvas_PointerReleased"
                    PointerCanceled="Canvas_PointerCanceled"
                    PointerWheelChanged="Canvas_PointerWheelChanged"
                    KeyDown="Canvas_KeyDown"
                    IsTabStop="True"
                    MinHeight="600"
                    MinWidth="800">
                    <Canvas.RenderTransform>
                        <CompositeTransform
                            ScaleX="{x:Bind ViewModel.Zoom, Mode=OneWay}"
                            ScaleY="{x:Bind ViewModel.Zoom, Mode=OneWay}"
                            TranslateX="{x:Bind ViewModel.PanX, Mode=OneWay}"
                            TranslateY="{x:Bind ViewModel.PanY, Mode=OneWay}" />
                    </Canvas.RenderTransform>

                    <ItemsControl ItemsSource="{x:Bind ViewModel.Icons, Mode=OneWay}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Y}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:QuestIconViewModel">
                                <Grid Width="{x:Bind Size}" Height="{x:Bind Size}" PointerPressed="Icon_PointerPressed" PointerReleased="Icon_PointerReleased" PointerCanceled="Icon_PointerCanceled">
                                    <Border
                                        CornerRadius="4"
                                        Background="{ThemeResource AccentFillColorSecondaryBrush}"
                                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                        BorderThickness="1">
                                        <Grid>
                                            <Rectangle Fill="{ThemeResource AccentFillColorTertiaryBrush}" Margin="4" RadiusX="6" RadiusY="6" />
                                            <TextBlock
                                                Text="{x:Bind Label}"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontSize="16"
                                                FontWeight="SemiBold" />
                                        </Grid>
                                    </Border>
                                    <Border
                                        CornerRadius="4"
                                        BorderBrush="{ThemeResource SystemControlForegroundAccentBrush}"
                                        BorderThickness="2"
                                        Visibility="{x:Bind IsSelected, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    <Border
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="4"
                                        Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}"
                                        CornerRadius="8"
                                        Padding="2,0"
                                        Visibility="{x:Bind HasBadge, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock
                                            Text="{x:Bind BadgeGlyph}"
                                            FontSize="12"
                                            FontWeight="Bold"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border
                        Canvas.Left="{x:Bind ViewModel.Selection.X, Mode=OneWay}"
                        Canvas.Top="{x:Bind ViewModel.Selection.Y, Mode=OneWay}"
                        Width="{x:Bind ViewModel.Selection.Width, Mode=OneWay}"
                        Height="{x:Bind ViewModel.Selection.Height, Mode=OneWay}">
                        <Border.BorderBrush>
                            <SolidColorBrush Color="{ThemeResource SystemAccentColor}" />
                        </Border.BorderBrush>
                        <Border.BorderThickness>
                            <Thickness>1</Thickness>
                        </Border.BorderThickness>
                        <Border.Visibility>
                            <x:Bind Path="ViewModel.Selection.HasArea" Mode="OneWay" Converter="{StaticResource BoolToVisibilityConverter}" />
                        </Border.Visibility>
                        <Border.Background>
                            <SolidColorBrush Color="{ThemeResource SystemAccentColorLight3}" Opacity="0.1" />
                        </Border.Background>
                    </Border>
                </Canvas>
            </Grid>
        </Border>
    </Grid>
</Page>
