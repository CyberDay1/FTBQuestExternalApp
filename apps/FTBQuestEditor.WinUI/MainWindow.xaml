<Window
    x:Class="FTBQuestEditor.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:FTBQuestEditor.WinUI.Views">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <InfoBar
            x:Name="ToastBar"
            Grid.Row="0"
            IsOpen="False"
            Severity="Informational"
            IsClosable="True"
            Margin="12,12,12,0" />

        <CommandBar x:Name="Toolbar" Grid.Row="1" DefaultLabelPosition="Right">
            <AppBarButton Icon="OpenFile" Label="Import" Click="OnImportClicked" />
            <AppBarButton Icon="AllApps" Label="Presets" Click="OnPresetsClicked" />
            <AppBarButton Icon="AllApps" Label="Loot Tables" Click="OnLootTablesClicked" />
            <AppBarButton Icon="Save" Label="Save" Click="OnSaveClicked" />
        </CommandBar>

        <views:ChapterNavigator x:Name="Navigator" Grid.Row="2" />
    </Grid>
</Window>
